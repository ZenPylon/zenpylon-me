language: node_js
jobs:
  include:
    - stage: 'lint'
      if: branch = dev OR branch = master
      name: 'Lint: Prettier'
      script: ./node_modules/prettier/bin-prettier.js
    - stage: 'build'
    - stage: 'test'
    - stage: 'deploy'
      if: branch = master
      script: "echo 'deploy here'"
